# PR #131 completion

Worktree: `openestates-pr131`, branch
`codex/pr-131-contextual-rails`. Preserve the inherited uncommitted changes.
Use `catalog-71-299c3135-23aa-4945-8f26-2cf2fc775eaa`; publishing to PR #131 was authorized after local verification.

Complete and verify in order:

1. Audit commits/configuration, rebuild the application, run journey/audit baselines,
   capture live identity, ordered IDs, predicates, proofs, collections, bytes/timing.
2. Preserve the existing signed start/revise/resume journey, stable typed predicates,
   exclusions and branch identities through clarification, retry, undo and navigation.
3. Correct AST-owned collections: wider budget, nearby, other; preserve requirements
   and exclusions, use canonical sourced geography, deduplicate societies, hydrate
   shelf rankings once, and optimize measured request costs without timeout changes.
4. Ship compact API cards and frontend consumers together; retain structured reasons,
   proof links and context across exact/contextual homes, workspace and compare.
5. Apply UI Critic/ThreeUI research; fix measured rail width, scrolling, resize,
   reveal and reduced-motion seams while retaining two About rows and Market trail.
6. Delete replaced implementations and proven orphans conservatively.
7. Extend existing contracts with non-empty rails, constraints, missing topology and
   API/UI navigation parity; run realistic live queries and final checks/captures.

Evidence and checkpoint outcomes: `docs/reviews/search-journey.md`.
Targets: unchanged three-second timeout; collections <80 KB uncompressed;
warm FCP <500 ms; landing CLS <0.05 and searched landing CLS <0.01.
